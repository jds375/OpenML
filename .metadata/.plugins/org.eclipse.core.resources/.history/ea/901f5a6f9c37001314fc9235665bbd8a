package knn;

import java.util.HashMap;

public class WeightedKNN extends UnweightedKNN {

	public WeightedKNN(DataSet trainingSet, int k,
			SimilarityMeasure similarityMeasure) {
		super(trainingSet, k, similarityMeasure);
	}
	
	@Override 
	protected HashMap<Double, Double> createVoteTable() {
		HashMap<Double, Double> voteTable = new HashMap<Double, Double>();
		for (int i = 0; i < getK(); i++) {
			double kNN = getTrainingSet().getDataSet()[getTrainingSet().getDataSet().length - i - 1];
			double kNNLabel = getTrainingSet().getDataSet()[getTrainingSet().getDataSet().length
					- i - 1].getLabel();
			if (voteTable.containsKey(kNNLabel)) {
				voteTable.put(kNNLabel, voteTable.get(kNNLabel) + 1);
			} else {
				voteTable.put(kNNLabel, 1.0);
			}
		}
		return voteTable;
	}

}
